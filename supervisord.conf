[supervisord]
nodaemon = true
loglevel = DEBUG

[program:redis]
command     = /usr/bin/redis-server /etc/redis/redis.conf --daemonize no --logfile ''
autostart   = true
autorestart = true
startsecs   = 10
priority    = 1
